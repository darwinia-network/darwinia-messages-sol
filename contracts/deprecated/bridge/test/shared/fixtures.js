const BeefyFixture = require('./beefy-fixture-data.json');
const MessageFixture = require('./message-fixture-data.json');
const GuardFixture = require('./guard-fixture-data.json')

module.exports = {
  BeefyFixture,
  MessageFixture,
  GuardFixture,
}
