{
  "name": "TimeLock",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ WormholeDao/TimeLock",
    "create-remote": "graph create --node http://localhost:8020/ WormholeDao/TimeLock",
    "create-local": "graph create --node http://localhost:8020/ WormholeDao/TimeLock",
    "remove-local": "graph remove --node http://localhost:8020/ WormholeDao/TimeLock",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 WormholeDao/TimeLock"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.25.1",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
